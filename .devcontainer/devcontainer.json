{
  "name": "thirdparty.ai.assistant.openclaw",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "customizations": {
    "vscode": {
      "settings": {
        "editor.formatOnSave": true
      },
      "extensions": [
        "eamodio.gitlens",
        "ms-azuretools.vscode-containers",
        "jebbs.plantuml",
        "openai.chatgpt",
        "trunk.io"
      ]
    }
  },
  "forwardPorts": [],
  "postStartCommand": "",
  "containerUser": "vscode",
  "runArgs": [
    "--userns=keep-id",
    "--add-host=gitea.internal:host-gateway"
  ],
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,Z",
  "workspaceFolder": "/workspace",
  "mounts": [
    "source=${localWorkspaceFolderBasename}-home,target=/home/vscode,type=volume"
  ],
  "postCreateCommand": "bash -lc 'bash /workspace/.devcontainer/postCreate.sh'"
}